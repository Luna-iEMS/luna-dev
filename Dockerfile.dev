# ------------------------------------------------------------------------------
# 🐳 Dockerfile.dev – Entwicklungsumgebung für Luna IEMS API & Worker
# ------------------------------------------------------------------------------

FROM python:3.11-slim

# Arbeitsverzeichnis
WORKDIR /app

# Systemabhängigkeiten installieren (für psycopg und Tika)
RUN apt-get update && apt-get install -y \
    build-essential \
    libpq-dev \
    curl \
    netcat-traditional \
    && rm -rf /var/lib/apt/lists/*

# Requirements kopieren und installieren
COPY ./api/requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt

# Quellcode kopieren
COPY ./api /app

# Port für FastAPI
EXPOSE 8000

# Default-Kommando (API)
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]



